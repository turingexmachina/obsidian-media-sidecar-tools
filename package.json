{
	"name": "obsidian-media-sidecar-tools",
	"version": "1.0.1",
	"description": "Create sidecar notes for media with Ctrl+Click, and hide the original media in file navigation if wanted.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"license": "MIT",
	"devDependencies": {
		"esbuild": "^0.28.1",
		"obsidian": "1.7.2",
		"typescript": "^7.0.2"
	},
	"allowScripts": {
		"esbuild@0.28.1": true
	}
}
